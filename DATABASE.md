# توثيق قاعدة البيانات (Database Documentation)

تعتمد المنصة على **Firebase Realtime Database** كقاعدة بيانات أساسية، بتصميم شجري (JSON Tree) يضمن سرعة الوصول للبيانات وتحديثها في الوقت الفعلي.

## 🌳 هيكل البيانات (Data Structure)

### 1️⃣ `users`
يحتوي على البيانات الأساسية لجميع المستخدمين (مدير، طالب، ولي أمر).
* `name`: الاسم الكامل.
* `email`: البريد الإلكتروني.
* `role`: الدور (admin, student, parent).
* `phone`: رقم الهاتف.
* `created_at`: تاريخ إنشاء الحساب (Timestamp).

### 2️⃣ `students_profiles`
بيانات إضافية خاصة بالطلاب فقط.
* `parent_id`: معرف ولي الأمر (UID).
* `total_stars`: مجموع النجوم الحالية.
* `group_id`: معرف المجموعة.
* `payment_type`: نوع الدفع (monthly/session).
* `base_price`: السعر المتفق عليه.

### 3️⃣ `sessions`
سجل الحصص الدراسية.
* `title`: عنوان الحصة.
* `date`: تاريخ الحصة.
* `group_id`: المجموعة المستهدفة.
* `is_closed`: هل تم إغلاق التعديل على الحصة.

### 4️⃣ `attendance`
سجل الحضور والغياب (مفهرس بمعرف الحصة).
* `status`: (present, absent, late).
* `timestamp`: وقت تسجيل الحضور.

### 5️⃣ `payments`
إدارة العمليات المالية لكل طالب.
* `amount`: المبلغ.
* `status`: (paid, partial, late).
* `type`: (monthly, session).

### 6️⃣ `homeworks` & `submissions`
إدارة الواجبات المدرسية وتسليمات الطلاب.

### 7️⃣ `evaluations`
التقييمات الأكاديمية والسلوكية الدورية.

### 8️⃣ `audit_logs`
سجل بجميع العمليات الحساسة التي قام بها المدير لضمان الموثوقية.

---

## 🚀 كيفية الاستيراد
1. اذهب إلى **Firebase Console**.
2. اختر مشروعك ثم انتقل إلى **Realtime Database**.
3. من القائمة (الثلاث نقاط) اختر **Import JSON**.
4. قم برفع ملف `database_schema.json` الموجود في جذر المشروع.
